# Input routing

Help loads references/help-text.md and stops. A clear request runs directly.
A brief folder or PROPOSAL.md uses step-01-ingest.md. An ambiguous path is
resolved from existing files before asking. Never require a brief for a fix.
If a brief points to its accepted proposal, execute the proposal once, not
both artifacts. Auto mode does not authorize agents or hazardous actions.
