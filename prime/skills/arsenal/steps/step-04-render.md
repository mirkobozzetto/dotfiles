# step 04 render

Render scripts/render.py only for --html or an explicit visual-delivery request. A Markdown roadmap does not require a browser. Opening is optional and an unavailable GUI is reported without repeated attempts.

The current SKILL.md owns the workflow policy. User consent is required
before any agent, reviewer or advisor. No automatic validation sessions
for prose; no continuation after a user stop or completed result.
