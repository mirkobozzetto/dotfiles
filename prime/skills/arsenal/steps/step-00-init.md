# step 00 init

Resolve the requested outcome from the description and existing conversation.
Separate advice, documents and implementation; preserve permission limits.
Agent install/uninstall flags and runtime extensions are unavailable in this Prime
phase; state that boundary and stop. Resolve -r as an existing roadmap, not a new
task. Otherwise return to SKILL.md's adaptive clarification and routing rules.
Create no output directory until the selected skill requires an artifact.

The current SKILL.md owns the workflow policy. Delegation and reviewers are
disabled. No automatic validation sessions for prose; no continuation after a
user stop or completed result. Prime Normal/Plan guards remain authoritative.
