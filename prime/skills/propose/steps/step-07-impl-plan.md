# step 07 impl plan

Write dependency-ordered implementation outcomes and proportionate verification. No arbitrary one-day, test-layer or parallelism quotas. Account for shared build resources as well as file overlap.

The current SKILL.md owns the workflow policy. User consent is required
before any agent, reviewer or advisor. No automatic validation sessions
for prose; no continuation after a user stop or completed result.
